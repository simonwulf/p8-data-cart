import { expect } from 'chai';

describe('integration: cart-parser + cart-writer', function() {
  it('should ...', function() {
    // TODO: add integration test for parser and writer working together
  });
});
